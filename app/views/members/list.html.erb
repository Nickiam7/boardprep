<div class="ui column twelve wide">
 <h1 style="text-align:left;">Current Members</h1>
    <table class="ui table collapsing segment">
      <thead>
        <tr>
          <th>Photo</th>
          <th>Name</th>
          <th>Title</th>
          <th>Sort</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @members.each do |member| %>
          <tr>
            <td><%= image_tag member.photo.url %></td>
            <td><strong><%= member.name %></strong></td>
            <td><strong><%= member.title %></strong></td>
            <td><strong><%= member.sort %></strong></td>
             <td><div class="ui tiny buttons">
              <div class="ui button"><%= link_to 'Edit', edit_member_path(member) %></div>
             <div class="or"></div>
              <div class="ui button "><%= link_to 'Delete', member, method: :delete, data: { confirm: 'Are you sure?' } %></div>
             </div></td>
          </tr>
        <% end %>
      </tbody>
        <tfoot>
        <tr>
        	<th colspan="7">
        		<div class="ui buttons">
                <%= link_to 'New Member', new_member_path, class: 'ui button linkedin' %>
    		        <%= link_to 'Sort List', root_path, class: 'ui button twitter' %>
            </div> 
            </th>
        </tr>
        </tfoot>	
    </table>
 </div>


        <div class="column four wide computer only structions">
          <h4 class="ui top attached header"><i class="circular info icon red small"></i> Hey, Look Over Here!</h4>

          <div class="ui segment attached">
            <p>New members will automatically appear in the sortable list on the <%= link_to 'Home page', root_path, style: 'color:#4092cc;' %> and on the list to the left.  Here, you can see all entered info, edit the info and delete the member.</p>
            <div class="ui list">
              <div class="item">
               <div class="content">
                <p>
                  <i class="circular warning icon red"></i> 
                  There is a small bug when returning to the <%= link_to "Sort List", root_path, style: 'color:#4092cc;' %>. The photos may not show up at all. A simple page refresh will fix this bug.</p>
               </div>
              </div>
            </div>
          </div>
          <div class="ui bottom attached header aligned center">
           <div class="ui buttons mini">
                <%= link_to 'New Member', new_member_path, class: 'ui button linkedin' %>
                <%= link_to 'Sort List', root_path, class: 'ui button twitter' %>
           </div> 
         </div>

        </div>

        <div class="mobile only column four wide structions">
         <div class="ui vertical buttons">
          <%= link_to '+ Member', new_member_path, class: 'ui button linkedin' %>
          <%= link_to 'Sort List', root_path, class: 'ui button twitter' %>
         </div>
        </div>

        <div class="tablet only column four wide structions">
         <div class="ui vertical buttons">
          <%= link_to '+ Member', new_member_path, class: 'ui button linkedin' %>
          <%= link_to 'Sort List', root_path, class: 'ui button twitter' %>
        </div>
        </div>
     

        

