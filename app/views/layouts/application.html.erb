<!DOCTYPE html>
<html>
<head>
  <title>ACHE Chairman and Board Members</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

<script>
$(function(){
  $('#Container').mixItUp();  
  $('.pop').popup({on: 'click'});
    $('.poop').popup({on: 'hover'});
});
</script>
</head>

<body>
  <div class="ui vertical icon menu blue visible-xs-inline-block visible-sm-inline-block" style="position:fixed; right:0px; top:275px;">
    <a class="item filter active twitter sort" data-sort="random">
      <i class="refresh icon"></i>
    </a>  
    <a class="item filter" data-filter="all">
      <i class="table icon"></i>
    </a>
    <a class="item filter" data-filter=".chair">
      <i class="block layout icon"></i>
    </a>
    <a class="item filter" data-filter=".gov">
      <i class="grid layout icon"></i>
    </a>
</div>

  <div class="ui grid aligned center">
    <div class="twelve column wide">
     <!--Devise Flash Div-->
    	<% flash.each do |name, msg| %>
    		<%= content_tag(:div, msg, class: "ui message blue") %>
    	<% end %> 
    </div>   
        <%= yield %>
        <%= render 'layouts/footer' %>
   </div>

</body>
</html>
